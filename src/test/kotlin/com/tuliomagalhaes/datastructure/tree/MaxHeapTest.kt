package com.tuliomagalhaes.datastructure.tree

internal class MaxHeapTest {

    private val initialList = listOf(41, 39, 33, 18, 27, 12)

    /*@Test
    fun insert_1_should_go_to_end_of_list() {
        // Given
        val maxHeap = MaxHeap(initialList)

        // When
        maxHeap.insert(1)

        // Then
        assertEquals(listOf(41, 39, 33, 18, 27, 12, 1), maxHeap.data)
    }

    @Test
    fun insert_199_should_go_to_top_of_list() {
        // Given
        val maxHeap = MaxHeap(initialList)

        // When
        maxHeap.insert(199)

        // Then
        assertEquals(listOf(199, 39, 41, 18, 27, 12, 33), maxHeap.data)
    }

    @Test
    fun insert_30_should_go_to_middle_of_list() {
        // Given
        val maxHeap = MaxHeap(initialList)

        // When
        maxHeap.insert(34)

        // Then
        assertEquals(listOf(41, 39, 34, 18, 27, 12, 33), maxHeap.data)
    }

    @Test
    fun max_should_return_root_and_reorder_items() {
        // Given
        val maxHeap = MaxHeap(initialList)

        // When
        val max = maxHeap.extractMax()

        // Then
        assertEquals(41, max)
        assertEquals(listOf(39, 27, 33, 18, 12), maxHeap.data)
    }*/
}